<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/BgColor">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/spacing_medium_small">

        <TextView
            android:id="@+id/tvAppInformation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawablePadding="@dimen/spacing_small_much"
            android:gravity="center"
            android:padding="@dimen/spacing_medium"
            android:textColor="@color/txtSecondColor"
            android:textSize="@dimen/txt_small"
            app:svgDrawable="@drawable/svg_about"
            app:svgTintColor="@color/blue"
            app:drawableType="svgType"
            app:iconLocationType="locationTop"
            tools:ignore="MissingPrefix" />

        <TextView
            android:id="@+id/tvAbout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@string/about_content" />

    </LinearLayout>
</android.support.v4.widget.NestedScrollView>
