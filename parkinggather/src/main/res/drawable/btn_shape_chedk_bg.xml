<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 统一 按钮 背景 （矩形） -->
    <solid android:color="@color/button_pressed" />

    <stroke
        android:width="@dimen/itemSplit_line"
        android:color="@color/lineColor" />
</shape>